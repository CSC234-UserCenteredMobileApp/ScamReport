# PR #35 — fix(call-screening): persist state, warn instead of block, fix stale prefs

| Field          | Value                                                                          |
| -------------- | ------------------------------------------------------------------------------ |
| Merged         | 2026-05-05                                                                    |
| Author         | AokDesu                                                                       |
| Diff size      | +579 / −63                                                    |
| Primary area   | mixed                                                                         |
| Surfaces       | none                                                                     |
| PR URL         | <https://github.com/CSC234-UserCenteredMobileApp/ScamReport/pull/35>                                                                        |
| CI verdict     | green (a PR cannot reach `main` unless every CI workflow on the run is green) — see PR URL for the full check matrix |

## Change summary

> **Bug 1 (critical):** `callScreeningEnabledProvider` was `StateProvider<bool>` hardcoded to `false` — never read from or wrote to SharedPreferences. Replaced with `AsyncNotifierProvider<CallScreeningEnabledNotifier, bool>` that loads initial state from `repo.isEnabled()` and persists on toggle via `repo.setEnabled()`. **Bug 2:** `getBlockedCalls()` read from Flutter's in-memory SharedPreferences cache without calling `prefs.reload()` first, so native writes from the Kotlin `ScamCallScreeningService` were never visible. Added `await prefs.reload()` before reading. **Bug 3:** `blockedCallsProvid

## Agent loop — what actually ran

### Plan Mode
- Session ID: n/a — session not captured in the local jsonl archive
- The merge predates the captured archive window (2026-05-22 onward) or ran in a Claude Code session that was not retained.
- The PR + its CI artefacts on GitHub remain authoritative.

### Engineer
- Mode: Claude Code session(s) authored the code changes.
- Local gauntlet expected before push: `bun run lint && bun run typecheck && bun run test` + `flutter test` per `apps/mobile/CLAUDE.md`.
- Diff scope: +579 / −63; full file list available at the PR URL.

### Architect review
- Mode: human (P2 Architect) — per `docs/ai-workflow.md` best-effort
- Evidence: PR review thread on GitHub (see PR URL).

### QA gates
- CI workflow: `.github/workflows/ci.yml` (api / mobile / shared jobs).
- Mobile job runs `flutter test --coverage`, mobile coverage threshold 80%.
- API + shared jobs run `bun test --coverage`, thresholds 80% / 90%.
- Verdict: pass (PR merged → all required checks green).

### Security review
- Surfaces touched: none — no auth / RBAC / Firestore / secrets / validation / uploads
- CI workflow ran the standing controls (gitleaks + bun audit + dart analyze)
- Agent: n/a — no security-relevant surfaces
- Verdict: pass

## Outcome

Merged on 2026-05-05 into `main`. Branch `fix/call-screening-persist-warn` → `main`.
